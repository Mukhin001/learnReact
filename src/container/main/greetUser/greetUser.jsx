import { useState } from "react";

const GreetUser = () => {
    const [showName, setActive] = useState(null);
        console.log(showName);
    return (
        <div className="greetUser"

        >
            
        </div>
    );
};

export default GreetUser;